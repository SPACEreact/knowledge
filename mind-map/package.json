{
  "name": "story-mind-map",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:contrast": "node scripts/contrast-audit.mjs",
    "check:site": "node scripts/site-audit.mjs",
    "check": "npm run check:contrast && npm run build -- --logLevel warn && npm run check:site",
    "optimize:reader": "node scripts/annotate-reader-images.mjs"
  },
  "dependencies": {
    "@orama/orama": "^3.1.18",
    "@preact/signals-core": "^1.13.0",
    "gsap": "^3.15.0",
    "mammoth": "^1.11.0",
    "three": "^0.182.0"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
